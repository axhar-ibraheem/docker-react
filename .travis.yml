sudo: required
services:
  - docker

before_install:
  - docker build -t axharibraheem/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true --name test-container axharibraheem/docker-react npm run test -- --watchAll=false
